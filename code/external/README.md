External code repositories as git submodules. Add existing code repos here rather than replacing code/.

Example:
  git submodule add https://github.com/user/repo code/external/repo
  git submodule update --init --recursive
